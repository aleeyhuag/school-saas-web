import {useState} from 'react';import {useNavigate,Link} from 'react-router-dom';import * as api from '../api/partner';import Button from '../components/ui/Button';import {Field,Input} from '../components/ui/FormFields';export default function PartnerLogin(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(null),nav=useNavigate();async function go(e){e.preventDefault();try{const d=await api.login({email,password});localStorage.setItem('partner_token',d.token);nav('/partner/dashboard')}catch(e){setError(e.response?.data?.message||'Unable to sign in.')}}return <div className="min-h-screen bg-bg flex items-center justify-center p-4"><form onSubmit={go} className="w-full max-w-sm bg-surface border border-border rounded-2xl p-6"><h1 className="text-2xl font-bold text-ink mb-5">Partner sign in</h1>{error&&<p className="text-sm text-danger mb-4">{error}</p>}<Field label="Email"><Input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></Field><Field label="Password"><Input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></Field><Button className="w-full">Sign in</Button><p className="text-xs text-muted text-center mt-4"><Link className="text-primary" to="/partner/register">Become a partner</Link></p></form></div>}
