import { useState } from 'react'; import { Link } from 'react-router-dom'; import * as partnerApi from '../api/partner'; import Button from '../components/ui/Button'; import { Field,Input } from '../components/ui/FormFields';
export default function ReferralPartnersPublic(){const [form,setForm]=useState({name:'',email:'',phone:'',password:'',password_confirmation:''});const [done,setDone]=useState(false);const [error,setError]=useState(null);const [submitting,setSubmitting]=useState(false);async function submit(e){e.preventDefault();if(submitting)return;setSubmitting(true);setError(null);try{const d=await partnerApi.register(form);localStorage.setItem('partner_token',d.token);setDone(true);}catch(e){setError(Object.values(e.response?.data?.errors||{}).flat()[0]||e.response?.data?.message||'Could not create your partner account.');}finally{setSubmitting(false);}}return <div className="min-h-screen bg-bg flex items-center justify-center p-4"><div className="w-full max-w-lg bg-surface border border-border rounded-2xl p-6 md:p-8">{done?<><h1 className="text-2xl font-bold text-ink">Partner account created</h1><p className="text-sm text-muted mt-2">Your referral code is ready. Open the partner dashboard to copy your link and track commissions.</p><Link className="inline-flex mt-5 bg-primary text-white px-5 py-3 rounded-xl" to="/partner/dashboard">Open partner dashboard</Link></>:<><h1 className="text-2xl font-bold text-ink">Become a Skulag Referral Partner</h1><p className="text-sm text-muted mt-2 mb-6">Refer schools from anywhere and track what you earn.</p>{error&&<p className="text-sm text-danger mb-4">{error}</p>}<form onSubmit={submit}><Field label="Full name"><Input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Email"><Input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="Phone (optional)"><Input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></Field><Field label="Password"><Input type="password" required minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></Field><Field label="Confirm password"><Input type="password" required minLength={8} value={form.password_confirmation} onChange={e=>setForm({...form,password_confirmation:e.target.value})}/></Field><Button type="submit" className="w-full" disabled={submitting} loading={submitting}>{submitting?'Creating account…':'Create partner account'}</Button></form><p className="text-xs text-muted text-center mt-4">Already a partner? <Link className="text-primary" to="/partner/login">Sign in</Link></p></>}</div></div>}